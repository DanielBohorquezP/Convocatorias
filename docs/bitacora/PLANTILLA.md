# Sesión NNN · AAAA-MM-DD

**Sprint:** N · día D de 30
**Duración aproximada:** —
**Commits:** `abc1234`, `def5678`

---

## Objetivo de la sesión

Qué se venía a hacer, según lo que decía `ESTADO.md` al abrir.

## Qué se hizo

Por requerimiento, no por archivo. Cada punto cita su `RF-xx`/`RNF-xx`.

- **RF-xx** — qué quedó funcionando.

## Cómo se verificó

La evidencia concreta: la consulta que se corrió, lo que devolvió, la pantalla que se revisó. Sin esto, la definición de terminado no se cumple (punto 5).

## Decisiones tomadas

Las que cambian el rumbo y no son obvias desde el código. Cada una con **qué se descartó y por qué**.

## Qué quedó abierto

Lo empezado y sin terminar, con la idea de por dónde seguir. Si no hay nada, se borra la sección.

## Hallazgos no planificados

Lo que apareció de paso y **no** se arregló sobre la marcha. Se copia también a `ESTADO.md`.

## Retrospectiva

*(Solo en la última sesión de cada sprint.)* Qué frenó, qué ajustar. Si sale una regla estable, se sube a `CLAUDE.md`.

## Estado al cerrar

- `ESTADO.md` actualizado: sí / no
- `docs/11-avance-por-requerimiento.md` actualizado: sí / no
- `tsc --noEmit` y `lint` limpios: sí / no
- Siguiente paso: —
